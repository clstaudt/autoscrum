# AutoScrum Team Configuration
#
# Team structure only — LLM settings come from environment variables:
#   AUTOSCRUM_LLM        Default model for all roles (LiteLLM format)
#   AUTOSCRUM_BASE_URL   Custom API endpoint (for oMLX, vLLM, Ollama, etc.)
#
# Per-role overrides (llm, base_url) can still be set here for advanced
# cases like running the Product Owner on a different model.

developer:
  count: 1
